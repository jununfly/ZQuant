# ZQuant Context

zettaranc 量化交易系统的 Python 复现——宏观活筹定方向 + 主线定赛道 + 量化定买卖点 + 纪律定仓位 的半自动趋势交易体系。本词汇表统一领域术语与架构概念，供 CLI/TUI/API/Flet 多端共用。

## Language

**仓位（Position）**:
资金分配的规划——总仓位上限、主线/支线/答应三层目标比例与金额。指"计划怎么分钱"，不含实际持有。
_Avoid_: 持仓、头寸

**持仓（Holdings）**:
实际持有的股票及其市值。与"仓位"（规划）相对，指"现在实际拿着什么"。
_Avoid_: 仓位、position

**盘态（Regime）**:
活跃市值（活筹）对大盘的择时判定，三态：多头 / 震荡 / 空头。
_Avoid_: 市场状态、regime（代码枚举名保留 `MarketRegime`）

**活筹（Active Capital）**:
活跃市值指数，系统的总开关择时数据源。
_Avoid_: 活跃市值（长名仅 PRD 全称使用）

## Language（补充）

**概览页（Overview）**:
Flet/UI 面向用户的第一页显示名。底层函数/API 保持 `status`（其返回系统状态）。显示名与技术名分层，各司其职。
_Avoid_: 状态页（仅 textual 面板内部沿用）、overview（技术层不用）

**答应（Defense 层）**:
仓位第三层，防御类资产（高股息/红利/公用/黄金）。中文术语用「答应」（zettaranc 原话），代码枚举 `defense`，文档标注「答应（防御层）」对应关系。
_Avoid_: 防御层（中文单独使用时不优先）、防御

**B 系列层级**:
B3 是系列/类别名（不单独触发），实际信号为 B3a（单针下20）与 B3b（砖型底部）。B1/B2 为独立信号。
_Avoid_: 把 B3 当独立可触发信号

**仓位双口径**:
- 层占比（如主线 50%-70%）：相对「总仓位」。
- 层金额/展示比例（如主线 48%）：相对「总资产」。
两处须明确标注，勿混用。
_Avoid_: 不加说明地混用两种百分比

## 架构概念

**指标层 vs 信号层**:
指标层（`indicators/`：活筹、KDJ、MA）产出指标值与盘态判定；信号层（`signals/`）检测 B/S/DD 交易信号。活筹盘态是指标的输出，不属于交易信号。
_Avoid_: 把盘态当信号

**接口清单（与实现一致）**:
`/api/health` `/api/status` `/api/scan` `/api/position` `/api/backtest`，以及待补的 `/api/active-cap`（文档已承诺，代码待加）。
_Avoid_: 文档引用未实现的路由
